@model  List<TenniOddMarathon.Models.TennisOdd>

@{
    ViewBag.Title = "Home Page";
}

<div> @Model.Count()</div>



@*<table>

    @foreach (var item in Model)
    {
    <tr>
        <td>
            @item.ParOne
        </td>
        <td>
            @item.ParTwo
        </td>
        <td>
            @item.Koefecienti.Count
        </td>

        @foreach (var item2 in item.Koefecienti)
    {

        <td>
            @item2.KoeficientFirst
        </td>
        <td>
            @item2.KoeficientSecond
        </td>
}



        <td>
            @item.TurnirDataPocetok
        </td>
    </tr>
    }
</table>*@




<table>
    @*<thead>
        <tr>
            <td>Пар еден</td>
            <td>Пар два</td>
            <td>Стартен коефициент</td>
            <td>Последен коефициент</td>
        </tr>
        
    </thead>*@
    @foreach (var item in Model)
    {
    <tr>
        @if (item.Koefecienti.Count() > 1)
    {
        <td>@item.ParOne</td>
        <td>@item.ParTwo</td>

        @*foreach (var item2 in item.Koefecienti)
        {
            <td>@item2.KoeficientFirst</td>
            <td>@item2.KoeficientFirst</td>
        }*@
        <td>@item.Koefecienti.FirstOrDefault().KoeficientFirst</td>
        <td>@item.Koefecienti.FirstOrDefault().KoeficientSecond</td>
       
        <td>@item.Koefecienti.LastOrDefault().KoeficientFirst</td>
        <td>@item.Koefecienti.LastOrDefault().KoeficientSecond</td>


}

    </tr>
    }
</table>